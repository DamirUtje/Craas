export interface Result {
  id: number;
  vorname: string;
  nachname: string;
  //..and so on
}

/*
export class Result {
  constructor(
    public id: number,
    public userId: number,
    public title: string,
    public body: string
  ) {}
}
*/
