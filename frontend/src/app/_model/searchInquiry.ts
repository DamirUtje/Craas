export class SearchInquiry {
  id: number;
  keyword: string;
  browserLanguage: string;
  mobileDevice: boolean;

  constructor(id: number, keyword: string, browserLanguage: string, mobileDevice: boolean) {
  }
}
